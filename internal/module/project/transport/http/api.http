### Project Module HTTP API 测试
### 使用前请确保服务器已启动，并替换 {{token}} 为实际的 JWT Token

@base_url = http://localhost:8080/api
@token = your_jwt_token_here

### 1. 创建项目
POST {{base_url}}/project
Content-Type: application/json
Authorization: Bearer {{token}}

{
  "name": "测试项目",
  "code": "TEST001",
  "description": "这是一个测试项目"
}

### 2. 获取项目列表（分页）
GET {{base_url}}/projects?pageNum=1&pageSize=10
Authorization: Bearer {{token}}

### 3. 根据 ID 获取项目
GET {{base_url}}/project/1
Authorization: Bearer {{token}}

### 4. 根据编码获取项目
GET {{base_url}}/project/code/TEST001
Authorization: Bearer {{token}}

### 5. 更新项目
PUT {{base_url}}/project/1
Content-Type: application/json
Authorization: Bearer {{token}}

{
  "name": "更新的测试项目",
  "description": "这是更新后的描述"
}

### 6. 变更项目状态 (0-待启动，1-进行中，2-已完成，3-已取消)
PUT {{base_url}}/project/1/status
Content-Type: application/json
Authorization: Bearer {{token}}

{
  "status": "2"
}

### 7. 删除项目
DELETE {{base_url}}/project/1
Authorization: Bearer {{token}}
